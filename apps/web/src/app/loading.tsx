export default function Loading() {
  return (
    <main className="grid min-h-screen place-items-center px-6">
      <p className="text-[var(--ruma-color-ink-muted)]">Loading RUMA…</p>
    </main>
  );
}
